# Heading Plugin

Renders heading blocks (H1–H6). Controls heading level, generates an anchor ID for linking, and contributes to the table of contents.

Future: custom heading styles per level, foldable headings.
