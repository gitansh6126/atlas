# Paragraph Plugin

The default block type. Renders standard text content. Supports inline formatting (bold, italic, links, code).

Future: rich text with inline style toggles, text alignment, line spacing.
