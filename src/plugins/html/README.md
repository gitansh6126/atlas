# HTML Plugin

Raw HTML embedding for advanced layouts. Sandboxed iframe rendering to prevent XSS. Includes code editor with syntax highlighting.

Future: live preview toggle, CSS scoping, embed presets.
